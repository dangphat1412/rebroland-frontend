import { Item } from "semantic-ui-react";
import styled from "styled-components";

export const NotificationItemContainer = styled(Item)`
  &:hover {
    background: rgba(0, 0, 0, 0.03) !important;
  }
`;
